---
name: videocut
description: 用于为 videocut 视频剪辑套件做顶层路由和新手引导。用户想处理口播、讲课、访谈或知识类视频，但不确定应该安装环境、语音剪辑、外挂字幕、烧录字幕、高清导出还是记录剪辑规则时，应使用本 skill。用户只说“处理这个视频”“怎么用 videocut”“我现在该做什么”时也应使用本 skill。
---

# Videocut 路由

> 判断用户现在处在哪一步，选择一个最小必要的子 skill，然后交接执行。

## 目标

把含糊的视频剪辑需求转成明确下一步：

- 新用户：解释推荐流程，并进入环境检查。
- 有原始口播视频：进入语音剪辑审核流程。
- 已有剪辑成片：进入外挂字幕、烧录字幕或高清化。
- 用户反馈识别偏好：进入自进化。
- 用户担心隐私：先处理上传边界，再继续。

本 skill 只做判断和交接，不复述子 skill 的实现细节。

## 触发正例

- “我想用 videocut 处理这个视频，但不知道从哪开始。”
- “这个口播视频帮我做成能发布的版本。”
- “我现在应该先语音剪辑、外挂字幕、烧录字幕还是高清化？”
- “第一次用这个 skill，要怎么配置？”
- “我已经剪完了，下一步做什么？”

## 不应触发

- 用户已经明确调用 `videocut:语音剪辑`、`videocut:外挂字幕`、`videocut:烧录字幕`、`videocut:高清化`、`videocut:安装` 或 `videocut:自进化`：直接读取对应子 skill。
- 用户只是问通用 FFmpeg、剪映或视频编码知识，且没有要使用 videocut 套件。
- 用户要求修改本仓库代码、README 或 GitHub 发布流程：按普通代码任务处理，不走视频剪辑路由。

## 快速路由表

| 用户目标 | 进入的 skill | 何时使用 |
| --- | --- | --- |
| 不知道从哪里开始、想做完整口播剪辑 | 本 skill | 给出推荐路径，再交接到第一个子 skill |
| 首次使用、环境准备、配置火山 API Key | `videocut:安装` | 没装依赖、没配置 `.env`、火山能力未开通 |
| 剪掉口误、卡顿、静音、重复句 | `videocut:语音剪辑` | 从原始口播视频生成审核页和剪辑结果 |
| 生成外挂字幕、导出 SRT/VTT、语义复核字幕 | `videocut:外挂字幕` | 只需要字幕文件，不把字幕烧进视频 |
| 生成字幕、校对字幕、烧录字幕 | `videocut:烧录字幕` | 需要把字幕烧进视频 |
| 导出更清晰、更适合发布的视频 | `videocut:高清化` | 剪辑/字幕完成后做最终高清导出 |
| 记录偏好、更新规则、改进 skill | `videocut:自进化` | 用户指出识别错了、规则要调整、习惯要沉淀 |

## 推荐完整流程

用户想做「一条完整口播成片」时，默认顺序是：

1. `videocut:安装`：确认依赖、火山 API Key 和模型能力。
2. `videocut:语音剪辑`：转录、识别口误/静音/重复，生成网页审核页。
3. 等用户在审核页确认并执行剪辑。
4. `videocut:外挂字幕`：生成、热词校对、语义复核并导出 SRT/VTT。
5. `videocut:烧录字幕`：如需要内嵌字幕，再生成、校对并烧录字幕。
6. `videocut:高清化`：按发布需求导出高清版本。
7. `videocut:自进化`：当用户给出偏好或纠错时再使用。

## 决策步骤

1. 识别用户是否已经指定子任务。已指定时直接交接对应子 skill。
2. 识别用户是否提供了视频路径。没有路径且下一步需要视频时，只问视频路径。
3. 判断环境是否可能未配置。首次使用、缺 API Key、火山模型未开通或依赖未知时，先交接 `videocut:安装`。
4. 判断当前素材阶段：
   - 原始口播视频：交接 `videocut:语音剪辑`。
   - 剪辑后视频但只要字幕文件：交接 `videocut:外挂字幕`。
   - 剪辑后视频且要内嵌字幕：交接 `videocut:烧录字幕`。
   - 已有最终内容但要提升画质：交接 `videocut:高清化`。
   - 用户在纠正规则或偏好：交接 `videocut:自进化`。
5. 如果用户只说“做成可发布版本”，说明推荐顺序，并从第一个可执行步骤开始。

## 缺信息时怎么问

只问当前步骤必需的问题：

- 没有视频路径：请用户提供视频文件路径。
- 环境可能没准备好：询问是否先运行安装检查，或直接检查本地依赖。
- 用户只说「处理一下」：推荐完整流程，并说明先从语音剪辑审核页开始。
- 用户强调隐私或敏感内容：询问是否使用可信音频 URL，避免默认上传器。

## 安全边界

- 默认语音剪辑流水线可能把提取出的音频上传到第三方临时文件服务；用户提到隐私、内部资料、未公开课程或敏感素材时，先提醒并建议使用可信 `--audio-url`。
- API Key 只写入本地 `.env`，不要写进 README、SKILL、日志或可提交文件。
- 执行剪辑删除片段前，应经过审核页或用户明确确认。
- 更新用户习惯或规则文件前，确认这是要沉淀为后续默认行为。

## 执行前加载

根据路由结果读取同仓库内对应文件：

- 安装：`安装/SKILL.md`
- 语音剪辑：`语音剪辑/SKILL.md`
- 外挂字幕：`外挂字幕/SKILL.md`
- 烧录字幕：`字幕/SKILL.md`
- 高清化：`高清化/SKILL.md`
- 自进化：`自进化/SKILL.md`

读取后按子 skill 的流程继续执行。
