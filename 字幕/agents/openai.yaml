interface:
  display_name: "烧录字幕"
  short_description: "生成、校对并烧录口播视频字幕"
  default_prompt: "Use $videocut:烧录字幕 to generate, correct, review, and burn subtitles for this video."

policy:
  allow_implicit_invocation: true
