interface:
  display_name: "语音剪辑"
  short_description: "转录口播视频并生成可审核剪辑方案"
  default_prompt: "Use $videocut:语音剪辑 to transcribe this spoken video, detect pauses and misreads, and generate a review page."

policy:
  allow_implicit_invocation: true
